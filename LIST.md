  git tag v0.5.0-beta
  git push origin v0.5.0-beta
  
  - loclm/config.json for loader settings like pause before launch, security scan on/off, runtime logging on/off, log
    level, and auto-open console.
  - Mod enable/disable system using enabled: true/false in modinfo.json or a loader config list.
  - Better in-game LOCLM menu tabs: Info, Mods, Security, Logs, Settings.
  - In-game mod list with status: loaded, blocked, failed, skipped, conflict warning.
  - Conflict report that shows when two mods patch the same script/object/event.
  - Security scan report file showing each scanned mod, hash, status, and reason if blocked.
  - “Copy logs” or “Open logs folder” helper for easier bug reports.
  - Automatic crash report bundle: LOCLM.log, LOCLM_proxy.log, LOCLM_runtime.log, mod list, hashes, game path.
  - Loader self-check at startup: missing files, wrong folder, missing .NET, bad data.win, no write permission.
  - First-run install verifier that prints “installed correctly” or exact missing file.
  - Better cache metadata file explaining why cache regenerated.
  - Cache rebuild reason in console: original game changed, loader changed, mod changed, config changed.
  - Mod load order display in console and in-game menu.
  - Mod priority conflict warnings when two mods have same priority.
  - Strict mod folder validation with clear errors.
  - Optional dependencies field in modinfo.json.
  - Optional incompatibleWith field in modinfo.json.
  - Optional gameVersion field in modinfo.json.
  - Optional loaderVersion minimum requirement in modinfo.json.
  - Mod API helper for adding main menu buttons safely without every mod patching the same GML.
  - Mod API helper for adding settings-menu entries.
  - Mod API helper for adding sprites/textures.✅
  - Mod API helper for replacing sounds. ✅
  - Mod API helper for adding included files. ✅
  - Mod API helper for safe string/text replacement. ✅
  - Shared button index allocator so mods do not fight over button IDs.
  - Shared menu/tab allocator for custom mod pages.
  - Better template examples: menu button mod, texture swap mod, sound replacement mod, config mod.
  - Separate “Example Mods” folder with tiny focused examples.
  - Mod template rename script so users can run rename-template.ps1 MyModName.
  - Build script for mod template that outputs a clean zip-ready mod folder.
  - GitHub Actions workflow that builds release zip automatically. ✅
  - GitHub Actions workflow that validates PR builds. ✅
  - GitHub Actions workflow that checks formatting. ✅
  - GitHub Actions workflow that uploads release assets when a tag is pushed. ✅
  - release.ps1 script that builds, trims, packages, and prints final file size.
  - clean.ps1 script that removes cache/build/output junk safely.
  - Better README install screenshots.
  - Wiki page for security scan and allowlist hashes.
  - Wiki page for mod conflicts.
  - Wiki page for creating texture mods.
  - Wiki page for creating menu mods.
  - Wiki page for release packaging.
  - GitHub issue templates for bug report, crash report, feature request, mod compatibility.
  - Pull request template with testing checklist.
  - CONTRIBUTING.md examples for good PR titles.
  - SECURITY.md explaining suspicious mod reporting.
  - Better Nexus description with install and troubleshooting sections.
  - Console theme config: simple, fancy, quiet.
  - Console progress bars for loading/scanning/building cache.
  - Better error summaries at the end instead of errors buried in logs.
  - “Safe mode” launch that disables all mods but keeps loader active.
  - “One mod at a time” debug mode for finding broken mods.
  - “Last known working mod set” metadata.
  - Back up previous working cache before replacing it.
  - Detect game update and warn that mods may break.
  - Detect duplicate mod DLLs.
  - Detect missing dependency DLLs.
  - Detect blocked files from Windows SmartScreen/Zone.Identifier.
  - Detect if user installed LOCLM into wrong folder.
  - Detect if version.dll is not next to LakeOfCreatures.exe.
  - Detect if Steam launched wrong executable.
  - Log exact command used to relaunch the game.
  - Add version info resource to version.dll.
  - Add file description/company/product metadata to binaries.
  - Sign release zips with SHA256 checksum file.
  - Generate checksums.txt for release.
  - Add manifest.json to release zip with loader version, build date, commit hash.
  - Add LOCLM_VERSION.txt into release folder.
  - In-game warning popup for blocked suspicious mods.
  - In-game warning popup for failed mods.
  - In-game warning popup when cache was regenerated.
  - Better in-game text layout for long paths.
  - Scrollable loaded mods list.
  - Scrollable failed mods list.
  - Copy mods path button.
  - Copy logs path button.
  - Open GitHub/wiki/Discord buttons if GameMaker URI support works safely.
  - Configurable hotkey to open LOCLM menu.
  - Runtime log levels from GML: info, warn, error.
  - Per-mod runtime log prefixes.
  - Mod API event hooks like OnPatch, OnLoaded, OnRuntimeInit.
  - Better exception handling around every mod load.
  - Timeout or isolation for risky mod scanning work.
  - More readable mod hash output.
  - security_allowlist.example.json.
  - CLI command --list-mods.
  - CLI command --scan-mods.
  - CLI command --rebuild-cache.
  - CLI command --safe-mode.
  - CLI command --version.
  - CLI command --doctor.
  - --doctor should print install status and common fixes.
  - More unit-style tests for metadata validation/security scanner.
  - Dry-run mode that patches data and exits without launching game.
  - Compare patched resources before/after per mod.
  - Emit mod_conflicts.json.
  - Emit loaded_mods.json.
  - Emit security_report.json.
  - Emit cache_manifest.json.
