// Replace this with real hook logic before enabling it in mod-config.json.
// If your target script takes arguments, pass them into #orig#(...).

show_debug_message("LOCLM example hook fired.");
#orig#();
